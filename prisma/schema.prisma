// This schema references the shared schema
// Run: npx prisma generate --schema=../../../packages/shared-prisma-schema/schema.prisma

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
